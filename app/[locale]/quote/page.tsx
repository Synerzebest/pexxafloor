import { Navbar, Footer, ProBadge } from '@/components';
import { SurfaceWrapper } from '@/components';

const page = () => {
    return (
        <>
          <Navbar />
          <ProBadge />
          <SurfaceWrapper />
          <Footer />  
        </>
    )
}

export default page
